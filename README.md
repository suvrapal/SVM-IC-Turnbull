The function "LR_int()" finds the left and right end points that contain the event time.

The function "data.int()" generates data for simulation study.

The function "em.svm.tb()" developes the EM algorithm.

